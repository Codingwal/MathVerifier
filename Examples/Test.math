/*
define ℤ:
    ∀a(∀b(a ∈ ℕ ∧ b ℕ ⇒ [a, b] ∈ ℤ))
{
    sorry
}

theorem Test(f):
    1 + 1 = 2
⇒ ∀x(x = x)
{
    let z: z ∈ ℕ × ℕ
    
    let a: ∃x([a, x] = z)
    let b: [a, b] = z
}
*/

define ∅:
    ¬∃x(x ∈ ∅)
{
    sorry
}

define SetEnumNotation:
    ∀a(∀b( ∀x(x ∈ {a, b} ⇔ x = a ∨ x = b) ))
{
    sorry
}

define Pair:
    ∀a(∀b( [a, b] = {a, {a, b}} ))
{
    sorry
}

theorem Test(a, b):
⇒ a = a
{

}